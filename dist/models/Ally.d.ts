export declare class Ally {
    id: number;
    name: string;
    tag: string;
    playerCount: number;
    points: number;
    rank: number;
    bashpoints: number;
    bashpointsRank: number;
    constructor(id: number, name: string, tag: string, playerCount: number, points: number, rank: number, bashpoints: number, bashpointsRank: number);
}
